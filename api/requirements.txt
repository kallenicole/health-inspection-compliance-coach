fastapi==0.112.2
uvicorn==0.30.6
pydantic==2.8.2
pandas==2.2.2
pyarrow==17.0.0
joblib==1.4.2
httpx==0.27.0
python-dotenv==1.0.1

# ETL deps — scikit-learn used by precompute_scores during nightly refresh only
# (not imported at API startup — scores are served from ml_scores.parquet)
requests>=2.31.0
h3>=4.1.0
scikit-learn==1.5.1
